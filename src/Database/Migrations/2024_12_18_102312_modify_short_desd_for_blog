use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     *
     * @return void
     */
    public function up()
    {
        // Modify the 'short_description' column to double its length
        Schema::table('blogs', function (Blueprint $table) {
            // Assuming the current length is 255 (default for string), we double it to 510
            $table->string('short_description', 510)->change();
        });
    }

    /**
     * Reverse the migrations.
     *
     * @return void
     */
    public function down()
    {
        // Revert the 'short_description' column to its original length (255)
        Schema::table('blogs', function (Blueprint $table) {
            $table->string('short_description', 255)->change();
        });
    }
};
